# Receiver sample

1. Import this sample from Package Manager.
2. Add `Omt Receiver` to a Quad or RawImage-style Renderer.
3. Use **Select** to pick an OMT source, or type `HOSTNAME (Source Name)`.
4. Enter Play Mode.

The receiver copies the latest BGRA frame onto the renderer texture property.
